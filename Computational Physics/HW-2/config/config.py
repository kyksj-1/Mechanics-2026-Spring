# -*- coding: utf-8 -*-
"""
系统级静态配置模块

定义项目中不变的路径、目录等系统级参数。
动态配置（如分辨率、迭代次数等）请使用 config.yaml。
"""

from pathlib import Path

# ============================================================
# 项目根目录（自动推断：config/ 的父目录即为项目根）
# ============================================================
PROJECT_ROOT: Path = Path(__file__).resolve().parent.parent

# ============================================================
# 各子目录路径
# ============================================================
DATA_DIR: Path = PROJECT_ROOT / "data"          # 原始数据目录
OUTPUT_DIR: Path = PROJECT_ROOT / "output"      # 输出结果目录（图片、日志等）
SRC_DIR: Path = PROJECT_ROOT / "src"            # 核心源代码目录
SCRIPTS_DIR: Path = PROJECT_ROOT / "scripts"    # 执行脚本目录
TESTS_DIR: Path = PROJECT_ROOT / "tests"        # 测试目录

# ============================================================
# 数据文件路径
# ============================================================
WAVEFORM_FILE: Path = DATA_DIR / "waveform.dat"

# ============================================================
# 确保输出目录存在
# ============================================================
OUTPUT_DIR.mkdir(parents=True, exist_ok=True)
